# Workflow

1. Fork & branch (`feat/...`, `fix/...`).
2. Add tests for new behavior.
3. Run `go test ./...`.
4. Update relevant package docs if public API changes.
5. Open PR with concise description & examples.
